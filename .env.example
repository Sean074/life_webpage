SECRET_KEY=
# Production: override this in the Dokploy environment tab with:
# python -c "import secrets; print(secrets.token_hex(32))"
HTTPS_ONLY=false
# Production: set HTTPS_ONLY=true in the Dokploy environment tab
